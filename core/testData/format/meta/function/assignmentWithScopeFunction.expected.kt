fun f() {
  name.sub = scope { x ->
    //
  }
  name.sub += scope { x ->
    //
  }
  name.sub -= scope { x ->
    //
  }
  name.sub *= scope { x ->
    //
  }
  name.sub /= scope { x ->
    //
  }
  name.sub %= scope { x ->
    //
  }
}

fun h() {
  long.name.sub =
      scope { x ->
        //
      }
  long.name.sub +=
      scope { x ->
        //
      }
  long.name.sub -=
      scope { x ->
        //
      }
  long.name.sub *=
      scope { x ->
        //
      }
  long.name.sub /=
      scope { x ->
        //
      }
  long.name.sub %=
      scope { x ->
        //
      }
}
