// MAX_WIDTH 26

fun compute(b: Boolean) {
  val c =
      if (a + b < 20)
          a + b
      else if (a < 20) a
      else
          a + b + b + 1000
  return if (a + b < 20)
      a + b
  else c
}
